#!/bin/bash
# # Use ioreg to retrieve information about connected USB devices
# usb_devices=$(ioreg -p IOUSB -l -w 0)
# echo "$usb_devices"
#
# # Check if an external keyboard is connected
# if echo "$usb_devices" | grep -q "Apple Internal Keyboard / Trackpad"; then
#   echo "External keyboard detected"
#   # Run a command specific to when an external keyboard is attached
# else
#   echo "External keyboard not detected"
#   # Run a command specific to when an external keyboard is not attached
# fi



